<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.backend.springbootdeveloper.mapper.PaymentMapper">

    <!-- 1. 주문 테이블 저장 -->
    <insert id="insertOrder" parameterType="map">
        INSERT INTO orders (
            order_id, user_id, email, order_status, created_at
        ) VALUES (
                     #{orderId}, #{userId}, #{email}, #{orderStatus}, NOW()
                 )
    </insert>

    <!-- 2. 토스 결제 테이블 저장 -->
    <insert id="insertTossPayment" parameterType="map">
        INSERT INTO toss_payment (
            payment_id, order_id, toss_order_id, toss_payment_key,
            toss_payment_method, toss_payment_status, requested_at, approved_at, total_amount
        ) VALUES (
                     #{paymentId}, #{orderId}, #{tossOrderId}, #{tossPaymentKey},
                     #{method}, #{status}, #{requestedAt}, #{approvedAt}, #{totalAmount}
                 )
    </insert>

    <!-- 3. 주문 상태 변경 -->
    <update id="updateOrderStatus">
        UPDATE orders
        SET order_status = #{status}
        WHERE order_id = #{orderId}
    </update>

    <!-- 4. 좌석 테이블 저장   -->
    <insert id="insertTrainSeat" parameterType="map">
        INSERT INTO train_seats (
            train_resv_id, seat_info
        ) VALUES (
                     #{orderId}, #{seatInfo}
                 )
    </insert>
</mapper>